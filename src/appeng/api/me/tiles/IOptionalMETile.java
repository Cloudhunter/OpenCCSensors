package appeng.api.me.tiles;

/*
 * Cables will connect, but signal will not propogate. ( IFulllyOptionalMETile disconnects )
 */
public interface IOptionalMETile {
	
	public boolean isEnabled();
	
}
